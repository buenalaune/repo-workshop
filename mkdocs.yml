site_name: repo_workshop
nav:
  - Home: index.md

theme:
  name: material

plugins:
  - search
  - mkdocstrings:
      project_name: repo_workshop
      handlers:
        python:
          options:
            docstring_style: numpy